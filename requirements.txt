# Backend
fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
alembic
pydantic
python-dotenv
PyJWT[crypto]  # verifies Clerk session JWTs against their JWKS - see backend/api/auth.py

# Scraping
requests
beautifulsoup4  # HTML description cleanup (scrapers/text_utils.py) - all 3 ATS integrations use JSON APIs, not browser automation

# Testing
pytest
httpx  # FastAPI's recommended test client for backend/api tests
